allow_login_shell = false

[features]
shell_snapshot = false

[shell_environment_policy]
inherit = "all"
exclude = ["UNRAID_MCP_TOKEN"]

[mcp_servers.unraid]
url = "__UNRAID_MCP_URL__"
bearer_token_env_var = "UNRAID_MCP_TOKEN"
